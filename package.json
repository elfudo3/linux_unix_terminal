{
  "name": "linux-unix-terminal",
  "version": "1.0.0",
  "description": "A browser-based virtual terminal for practising Linux/Unix commands.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^30.1.1",
    "typescript": "^5.8.3",
    "vite": "^7.1.0",
    "vitest": "^5.0.1"
  }
}
